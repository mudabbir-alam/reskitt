---
const { href, moreClasses } = Astro.props;
const Tag = href ? "a" : "button";
const attrs = href ? { href } : { type: "button" as const };
---

<Tag
    {...attrs}
    class=`px-6 py-4 bg-primary rounded-lg mt-4 text-center text-xl ${moreClasses}`
>
    <slot />
</Tag>
