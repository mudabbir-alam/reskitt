---
const { id, heading } = Astro.props;
---

<section id={id} class="py-8 border-t-2 border-border scroll-mt-20">
    <h2 class="mb-4 font-black text-2xl dark:text-slate-300">{heading}</h2>
    <div class="space-y-2 text-slate-600 dark:text-slate-400">
        <slot />
    </div>
</section>
